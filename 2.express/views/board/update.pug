extends ../layout.pug 

block styles

block header
  include ../include/header.pug
block content 
  h1='Board Update'
  
  form(method='POST', action=`/board/update/${data.bo_no}`)
    p
      input(type='text', name='bo_subject', value=data.bo_subject)
    p
      textarea(name='bo_insert_content')=data.bo_insert_content
    p
      button(type='submit')='전송'
block footer
  include ../include/footer.pug